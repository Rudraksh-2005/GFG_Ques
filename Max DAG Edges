class Solution {
  public:
    int maxEdgesToAdd(int V, vector<vector<int>>& edges) {
        // code here
        int E = edges.size(); 
        int maxPossible = (V * (V - 1)) / 2; 
        return maxPossible - E;
    }
};
